{
	"name": "dataflow_silver_to_gold_v2",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"name": "readOrders"
				},
				{
					"name": "readProducts"
				},
				{
					"name": "customers"
				}
			],
			"sinks": [
				{
					"name": "customerSalesAmount"
				}
			],
			"transformations": [
				{
					"name": "joinOrdersProducts"
				},
				{
					"name": "selectRequiredColumns"
				},
				{
					"name": "JoinOrderDetailsCustomers"
				},
				{
					"name": "aggCustomerOrderTotalAmount"
				}
			],
			"scriptLines": [
				"source(output(",
				"          order_id as string,",
				"          order_date as date,",
				"          customer_id as string,",
				"          product_id as string,",
				"          quantity as short,",
				"          rn as boolean",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     wildcardPaths:['orders/*.csv']) ~> readOrders",
				"source(output(",
				"          product_id as string,",
				"          product_name as string,",
				"          category as string,",
				"          price as double,",
				"          rn as boolean",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     wildcardPaths:['products/*.csv']) ~> readProducts",
				"source(output(",
				"          customer_id as string,",
				"          name as string,",
				"          city as string,",
				"          rn as boolean",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     wildcardPaths:['customers/*.csv']) ~> customers",
				"readOrders, readProducts join(readOrders@product_id == readProducts@product_id,",
				"     joinType:'inner',",
				"     matchType:'exact',",
				"     ignoreSpaces: false,",
				"     broadcast: 'auto')~> joinOrdersProducts",
				"joinOrdersProducts select(mapColumn(",
				"          order_id,",
				"          order_date,",
				"          quantity,",
				"          product_name,",
				"          price,",
				"          customer_id",
				"     ),",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> selectRequiredColumns",
				"selectRequiredColumns, customers join(selectRequiredColumns@customer_id == customers@customer_id,",
				"     joinType:'inner',",
				"     matchType:'exact',",
				"     ignoreSpaces: false,",
				"     broadcast: 'auto')~> JoinOrderDetailsCustomers",
				"JoinOrderDetailsCustomers aggregate(groupBy(customers@customer_id,",
				"          name),",
				"     totalAmont = sum(quantity*price)) ~> aggCustomerOrderTotalAmount",
				"aggCustomerOrderTotalAmount sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     preCommands: [],",
				"     postCommands: []) ~> customerSalesAmount"
			]
		}
	}
}