{
	"name": "flowlet1",
	"properties": {
		"type": "Flowlet",
		"typeProperties": {
			"sources": [],
			"sinks": [],
			"transformations": [
				{
					"name": "window"
				},
				{
					"name": "filter1"
				},
				{
					"name": "select1"
				},
				{
					"name": "derivedKey"
				},
				{
					"name": "input"
				},
				{
					"name": "output1"
				}
			],
			"scriptLines": [
				"parameters{",
				"     pKey as string",
				"}",
				"input(order: 0,",
				"     allowSchemaDrift: true) ~> input",
				"derivedKey window(over(keyCol),",
				"     asc(keyCol, true),",
				"     rn = rowNumber()) ~> window",
				"window filter(rn == 1) ~> filter1",
				"filter1 select(mapColumn(",
				"          each(match(name != 'keyCol' && name != 'rn'))",
				"     ),",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> select1",
				"input derive(keyCol = toString(byName($pKey))) ~> derivedKey",
				"select1 output() ~> output1"
			]
		}
	}
}